
## TEST PROJECT

```
Usage: ol2config [-?fnqvV] [--auto] [--bash] [--check] [--compact] [--csh]
                 [--dump-config] [--json] [--json5] [--jsonc] [--keep-comments]
                 [--minify] [--ol1] [--ol2] [--pretty] [--properties]
                 [--remove-comments] [--sho-autocomplete] [--show-sbom] [--tcl]
                 [--toml] [--validate] [--warn-only] [--xml] [--yaml]
                 [--config=configPath] [-o=outputPath] [inputPath] [outputPath]
OpenLane2 configuration file tool
      [inputPath]           Source Config File
      [outputPath]          Output file (- for stdout)
  -?, --help                Show help
      --auto                Automatic conversion
      --bash, --export-bash, --export-shell-bash
                            Output SHELL-CSH
      --check
      --compact, --format-compact
                            Compact formatted output
      --config=configPath   Config file
      --csh, --export-csh, --export-shell-csh
                            Output SHELL-BASH
      --dump-config         Dump internal config
  -f, --force               -f force overwrite,
                            -ff even if same content
      --json, --export-json Output JSON
      --json5, --export-json5
                            Output JSON5
      --jsonc, --export-jsonc
                            Output JSONC
      --keep-comments, --output-comment-keep
                            Keep comments in output
      --minify, --format-minify
                            Minify formatted output
  -n, --dry-run             Just report, no modification
  -o, --output=outputPath   Output file (- for stdout)
      --ol1, --openlane1    Target OpenLane1
      --ol2, --openlane2    Target OpenLane2
      --pretty, --format-pretty
                            Pretty formatted output
      --properties, --export-properties
                            Output PROPERTIES
  -q, --quiet               Quiet
      --remove-comments, --output-comment-remove
                            Remove comments from output
      --show-autocomplete   Show CLI autocomplete script
      --show-sbom           Show SBOM
      --tcl, --export-tcl   Output TCL
      --toml, --export-toml Output TOML
  -v, --verbose             Verbose
  -V, --version             Show Version
      --validate
      --warn-only
      --xml, --export-xml   Output XML
      --yaml, --export-yaml Output YAML
```
